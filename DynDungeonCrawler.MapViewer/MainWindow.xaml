<Window x:Class="DynDungeonCrawler.MapViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DynDungeonCrawler.MapViewer"
        mc:Ignorable="d"
        Title="Dungeon Map Viewer" Height="600" Width="900">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="BtnLoadDungeon" Content="Load Dungeon JSON..." Width="160" Margin="0,0,10,0" Click="BtnLoadDungeon_Click" />
        </StackPanel>
        <Grid Grid.Row="1" Margin="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <GroupBox Header="Paths Only" Grid.Column="0" Margin="0,0,5,0">
                <RichTextBox x:Name="RtbMapDisplayPaths"
                             FontFamily="Consolas"
                             FontSize="16"
                             IsReadOnly="True"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             Background="Black"
                             Foreground="White" />
            </GroupBox>
            <GroupBox Header="With Entities" Grid.Column="1" Margin="5,0,0,0">
                <RichTextBox x:Name="RtbMapDisplayEntities"
                             FontFamily="Consolas"
                             FontSize="16"
                             IsReadOnly="True"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             Background="Black"
                             Foreground="White" />
            </GroupBox>
        </Grid>
    </Grid>
</Window>